# Readme arquitectura raiz estructura wordflow code

Repo: `maxbry123-commits/nct-core` · rama `main`  
Uso: llenar `schema: tel.workflow/v3` para Wordflow Code / NCT.

## Indice raiz (nct-core)

1. `Frontend code NCT` — zips UI/fromtend + Cline + Aider + MUSE + manifest.
2. `skills agente nct` — Wordflow Code + Skills LOOP (copia desde agentes).
3. `Documentos proyectos nct` — anexos wordflow docs.
4. `Download code NCT` — inbox zips NCT.
5. `NCT neuronas code turbo` — raiz NCT.
6. `Desplegar nct` / `Refactoria nct` / `PIPELINE nct` / `Método de trabajo nct` / `Agente motores nct` — org T2.
7. `code-programming-engine` — motor programar (instance_pool, catalogs). Vive aqui.
8. `Método de trabajo` — metodo vivo.
9. `contracts` / `scripts` / `.github` / `Core` — contratos, packer A/B, GHA.

## Contrato tel.workflow/v3

```yaml
schema: tel.workflow/v3
repo: maxbry123-commits/nct-core
branch: main
wordflow_code_root: skills agente nct/Wordflow Code
frontend_zips: Frontend code NCT
engine: code-programming-engine
docs_root: Documentos proyectos nct
```
