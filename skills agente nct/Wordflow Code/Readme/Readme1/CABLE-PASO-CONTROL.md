# CABLE Wordflow Code <- wordflow-paso-control

Ancla YAML `skills/wordflow-paso-control/references/RULES.yaml`.
CABLE A = arquitectura (no editar code_path_runner.py).
CABLE B = apply_push al final del path.
DAG omit_steps false.
Token env Maxbry_123_tokens.
